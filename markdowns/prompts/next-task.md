# Prompt: Next Task (ScannrAI)
You are continuing implementation of **ScannrAI**. Use `markdowns/development-plan.md` as the source of truth.

## Step
1. Read `markdowns/development-plan.md`.
2. Find the next unchecked task in the current milestone.
3. Implement ONLY that task (and any minimal dependencies required).
4. Run tests/lint if available and ensure the app still runs with `docker compose up`.

## Progress update
After finishing:
- Check the task box in `markdowns/development-plan.md`
- Append a short entry to `markdowns/progress.md`:
  - What you implemented
  - Files changed
  - Any commands run
  - Next task to do
